# prompt 管理器软件
## 动机

现在程序员vibe coding很流行，而且vibe coding的频率很高。

但是ai有的时候不会遵循指令或者完全按照开发者的想法来做，就导致开发者经常需要中间介入来不断提出要求修改，所以说就会经常需要输入prompt。

很多时候，prompt中的部分内容是可以复用的，因为你要不断给AI强调核心的上下文，以防其因为上下文窗口满了，或者指令遵循忘了重点。因此，prompt的存储很重要。

而且，一般来说prompt都要用markdown格式写，即所谓的下面这种案例的样子：

```
## 任务
在 policy.py 的 get_action_chunk() 返回之后、execute() 调用之前，
插入一个 DMP 轨迹平滑后处理步骤。

## 背景事实（已确认，不需要重新探索）
- 当前接口：get_action_chunk(self, obs) -> np.ndarray, shape (T, 7)
- 动作空间：7 维 = [x, y, z, qx, qy, qz, gripper]，已归一化到 [-1, 1]
- 相关文件：
  - policy.py（推理主流程，get_action_chunk 在第 42 行）
  - execute.py（动作执行，不修改）
  - dmp_smooth.py（不存在，需要新建）

## 约束
- 允许修改：policy.py（仅插入调用）、新建 dmp_smooth.py
- 禁止修改：execute.py、action space 维度、归一化方式、action chunk 长度 T
- 禁止行为：
  - 新增测试文件或 probe 脚本
  - 重构 get_action_chunk 内部逻辑
  - 改变 DMP 的 canonical system 类型（用离散，不用 rhythmic）
- DMP 权重：从 weights/dmp_weights.npz 加载，不允许运行时重新拟合

## 终点线（完成标准）
1. get_action_chunk 的返回 shape 仍为 (T, 7)，dtype 不变
2. 平滑后末端目标点与原始输出的欧氏距离 < 0.01（归一化单位）
3. 运行 python -c "from policy import Policy; p=Policy(); a=p.get_action_chunk(None); print(a.shape)" 输出 (T, 7)

## 收尾输出
- 改动文件列表
- 上述验证命令的实际输出
- 如果 DMP 权重文件不存在，明确报告阻塞，不要自己生成随机权重
```

这种形式会使得ai更加按照指令工作，指令遵循能力大大提升，减少返工率，所以说prompt就得是markdown的格式。

但是因为开发过程中是很繁忙的，所以说很多时候不会非常好地去整理prompt。一个一个markdown记下来又太占空间，因为这样一天就能产生海量的markdown文件。这样在开发过程中就会分心到prompt整理中去，但是实际上大部分prompt确实是用完就不会再用第二次的，只是少数有复用需求。如果全部prompt用了就丢，那有很不方便，尤其是复用的部分不能直接复制粘贴了，查找历史也不方便。

实际上我发现把prompt发给Telegram或者飞书的群聊是很方便的。但是这样短期没问题，长期的查找又麻烦。而且这样聊天不断上滑翻阅也很麻烦，尤其是prompt内容很长的时候。

因此，有一种需求，那就是自动化整理prompt的工具。他可以解决上面这些痛点，实现无感，但是又整洁，而且符合markdown的格式。

---

## 目的

便于在工作的时候集中精神，不会因为这个分心，无需花精力去管理prompt，又很方便查询历史。

---

## 任务

### A. 聊天窗口化

A1. 实现类似飞书、Telegram 群聊的聊天窗口效果：一个聊天窗口，允许想到什么就往里写什么。写的不是 chat 信息，而是 prompt。这样对灵感快速记录很有效，且不容易分心。

A2. 信息气泡中允许对气泡加标签，标签可自定义分类，以便查找和后续过滤器直接过滤。

A3. 气泡内要能直接进行 markdown 渲染，即实时输入、实时渲染，效果类似 Typora / MarkText。

A4. 聊天窗口允许再度编辑已发送的气泡，允许删除、调整气泡顺序。

A5. 聊天窗口底部输入框相当于一个 markdown 编辑器，直接在聊天框输入就和聊天一样。输入框内要支持 markdown 实时渲染，即边编辑、边渲染。

A6. 输入框旁边有一个“长编辑器”按钮。点击后弹出一个小窗口 markdown 编辑器（不覆盖整个聊天窗口，类似便利贴），可进行详细、长内容的编辑，乃至上下滑动查看。关闭小窗后内容回填到输入框，不丢失。

A7. 聊天窗口内部支持查询，查询范围为当前窗口内所有内容（气泡正文 + 标签）。

### B. 自动化整理

B1. 当天的 prompt 集中在一个窗口里，即自动每日生成一个叫“今天”的聊天窗口。工作时就在这个窗口里编辑，输入框发送后进入“今天”窗口。

B2. 以本地时间 00:00 为界，跨天后自动完成：
   - 原“今天”内容迁移为“昨天”窗口；
   - 生成新的“今天”窗口并清空；
   - 原“昨天”顺延为“前天”。

B3. “昨天”“前天”窗口提供侧边大纲，列出每个聊天气泡的标题；点击大纲项可快速跳转到对应气泡。

B4. 聊天窗口列表只保留“今天 / 昨天 / 前天”三个。第 4 天起，内容自动归档进“历史数据库”，不再出现在聊天窗口列表。

B5. 历史数据库支持按 **年 → 月 → 日** 层级进入查询。默认直接进入“当年当月”，因为大部分查询都是查最近的。要查看别的月份或年份时，再手动回到上一层入口。

B6. 支持为“周”添加标签与工作内容简介。周与日期层级平级，不作为独立导航层级。
   示例：2026 年第 37 周可加标签“DMP 平滑重构”，它不参与导航层级，只在查询时作为筛选维度。

B7. 所有聊天窗口（今天 / 昨天 / 前天 / 历史）都支持查询、添加标签、编辑已发送的 prompt 气泡。

B8. 跨天迁移与归档在应用未运行时跨天后再次启动也能正确补齐（不依赖应用必须整夜开着）。

### C. 主题与跨平台

C1. 提供深色和浅色两套主题，允许手动切换，也允许根据系统默认设置切换。

C2. 在 Debian/Ubuntu、macOS on Apple Silicon、Windows 三种平台上，跟随系统主题均生效。

---

## 背景

- 完全从 0 开始开发，目前没有任何已开发完成的工程。
- 目标用户是单用户（开发者自己），本地工具，不做账号体系。
- 使用频率：每天多次。
- 数据量级预估：每天几十条 prompt，单条几百到几千字。
- 检索需求：需要全文检索，但对性能要求不高，能覆盖数年数据即可。

---

## 约束

- 使用flutter开发。
- 必须跨平台，支持arm、x86平台。一个app走遍全部硬件平台。
- 全部是64位的支持，不必支持32位。
- 一个app支持多种操作系统，包含debian（ubuntu）、mac os on arm（M系芯片）、Windows（x86）。
- 需要有深色模式和浅色模式两种主体，并且允许手动切换和根据系统默认设置进行切换（且支持上述的多种操作系统平台都能实现根据系统默认设置切换）
- 路径要使用api开发，因为多平台的路径写法不一样，尤其是Windows比较奇葩
- 要支持自定义历史文件备份（即“历史数据库”）的路径自定义（尤其是用户可以把它们保存到本地云同步文件夹里实现云同步）

---

## 终点线（完成标准）

### 1. 工程与构建
1. 项目可从零 `flutter create` 后按 README 步骤一次性跑通，无隐藏手动步骤。
2. `flutter analyze` 无 error；warning 数量为 0 或已在 README 中逐条说明。
3. `flutter test` 全部通过（至少覆盖：日期窗口迁移逻辑、标签过滤、Markdown 渲染、历史数据库读写、路径抽象）。
4. 三个目标平台均可构建并启动成功：
   - Debian/Ubuntu（x86_64）
   - macOS on Apple Silicon（arm64）
   - Windows（x86_64）
5. 仅支持 64 位；不引入任何 32 位专属依赖。
6. 所有文件路径均通过路径抽象 API 获取，代码中不出现硬编码的 `/`、`C:\`、`~` 等平台相关路径拼接。
7. 历史数据库存储路径可在设置中自定义；切换到云同步目录（如 iCloud Drive / OneDrive / Dropbox 同步文件夹）后，重启应用数据仍可正确读取与写入。

### 2. 聊天窗口化
8. 主界面为聊天窗口形态：底部输入框 + 消息气泡列表，交互逻辑类似 Telegram/飞书。
9. 输入框支持 Markdown 实时渲染（边输入边渲染），发送后气泡内也按 Markdown 渲染。
10. 输入框旁有“长编辑器”按钮，点击弹出**非全屏**小窗 Markdown 编辑器（便利贴式），可滚动、可实时预览；关闭后内容回填到输入框且不丢失。
11. 已发送的气泡支持：再次编辑、删除、调整顺序（至少支持上移/下移）。
12. 每个气泡可添加/移除自定义标签；标签支持自定义分类与颜色。
13. 聊天窗口内支持全文查询，查询范围为当前窗口内所有气泡内容与标签。
14. 删除气泡、编辑气泡、调整顺序后，界面与持久化数据保持一致，重启后不丢失。

### 3. 自动化整理
15. 应用启动后自动存在“今天”窗口，当天发送的 prompt 默认进入该窗口。
16. 以本地时间 00:00 为界，跨天后自动完成：
    - 原“今天”内容迁移为“昨天”窗口；
    - 生成新的“今天”窗口并清空；
    - 原“昨天”顺延为“前天”。
17. “昨天”“前天”窗口提供侧边大纲，列出每个气泡的标题；点击大纲项可跳转到对应气泡。
18. 超过“前天”的窗口不再出现在聊天窗口列表，而是自动归档进“历史数据库”。
19. 历史数据库支持按 **年 → 月 → 日** 层级进入查询；默认直接进入“当年当月”。
20. 支持为“周”添加标签与工作内容简介；周与日期层级平级，不作为独立导航层级。
21. 所有聊天窗口（今天/昨天/前天/历史）均支持查询、添加标签、编辑已发送气泡。
22. 跨天迁移、归档、周标签在应用未运行时跨天后再次启动也能正确补齐（即不依赖应用必须整夜开着）。

### 4. 主题与跨平台体验
23. 提供深色/浅色两套主题，支持手动切换与跟随系统；在 Debian/Ubuntu、macOS、Windows 上跟随系统均生效。
24. 主题切换后无需重启，界面即时更新；重启后保留用户选择。
25. 三平台下窗口基本布局、字体、滚动、输入法行为无明显错位或崩溃。

### 5. 数据安全与健壮性
26. 所有 prompt 数据本地持久化，应用崩溃或强杀后重启，已发送内容不丢失。
27. 历史数据库路径不可写/不存在时，给出明确错误提示，不静默失败、不生成随机数据。
28. 对空输入、超长输入、纯符号输入、含代码块输入均有合理处理，不崩溃。
29. 提供最小可用的导入/导出或备份机制（至少能手动备份历史数据库目录并恢复）。

### 6. 验证方式（必须实际执行并贴出结果）
- `flutter analyze` 输出
- `flutter test` 输出
- 三平台各一次启动截图或日志
- 跨天迁移的模拟验证（可通过修改系统时间或注入时钟依赖完成）
- 自定义历史路径后重启读取验证
- 跟随系统主题切换验证（三平台各一次）

---

## 收尾输出

请按以下结构输出，不要只写“已完成”：

1. 改动/新增文件列表
   - 每个文件一句话说明用途

2. 关键设计决策
   - 日期窗口迁移与归档的实现方式（含跨天补齐逻辑）
   - 历史数据库存储格式与路径抽象方案
   - Markdown 实时渲染方案选型
   - 主题跟随系统的跨平台实现方式
   - 标签与周标签的数据模型

3. 验证命令与实际输出
   - `flutter analyze` 实际输出
   - `flutter test` 实际输出
   - 三平台构建/启动命令与实际结果
   - 跨天迁移模拟的实际结果
   - 自定义历史路径后重启读取的实际结果
   - 跟随系统主题切换的实际结果

4. 已知限制与未完成项
   - 明确列出哪些终点线未完全满足，原因是什么
   - 明确列出哪些平台未实测，只有理论支持

5. 阻塞报告
   - 如果遇到无法解决的依赖、平台限制、权限问题，明确报告阻塞点
   - 不要自行降级需求、不要生成假数据、不要跳过验证

6. 下一步建议
   - 如果继续迭代，优先级最高的 3 件事
